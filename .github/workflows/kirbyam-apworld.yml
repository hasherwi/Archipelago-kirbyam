name: KirbyAM APWorld

on:
  push:
    branches:
      - main
    tags:
      - "kirbyam-v*"
  pull_request:
    paths:
      - ".github/workflows/kirbyam-apworld.yml"
      - ".github/scripts/kirbyam_release_metadata.py"
      - "worlds/kirbyam/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-apworld:
    runs-on: ubuntu-latest
    outputs:
      apworld_name: ${{ steps.package.outputs.apworld_name }}
      release_name: ${{ steps.meta.outputs.release_name }}
      release_tag: ${{ steps.meta.outputs.release_tag }}
      release_enabled: ${{ steps.meta.outputs.release_enabled }}
      version: ${{ steps.meta.outputs.version }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Show Python version
        run: |
          python --version

      - name: Prepare release metadata
        id: meta
        run: |
          python .github/scripts/kirbyam_release_metadata.py \
            --github-ref "${GITHUB_REF}" \
            --world-manifest "worlds/kirbyam/archipelago.json" \
            --github-output "${GITHUB_OUTPUT}"

      - name: Install release patch build dependencies
        if: steps.meta.outputs.release_enabled == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y make gcc-arm-none-eabi binutils-arm-none-eabi
          python -m pip install --upgrade pip
          python -m pip install bsdiff4

      - name: Materialize clean release ROM from secret
        if: steps.meta.outputs.release_enabled == 'true'
        shell: bash
        env:
          KIRBYAM_BASE_ROM_B64: ${{ secrets.KIRBYAM_BASE_ROM_B64 }}
        run: |
          if [ -z "${KIRBYAM_BASE_ROM_B64}" ]; then
            echo "Missing required GitHub secret KIRBYAM_BASE_ROM_B64 for tag-driven KirbyAM releases." >&2
            echo "Store the clean USA kirby.gba bytes as a base64-encoded repository secret." >&2
            exit 1
          fi

          printf '%s' "${KIRBYAM_BASE_ROM_B64}" | base64 --decode > worlds/kirbyam/kirby_ap_payload/kirby.gba
          ls -lh worlds/kirbyam/kirby_ap_payload/kirby.gba

      - name: Build kirbyam.apworld for branch and PR validation
        if: steps.meta.outputs.release_enabled != 'true'
        working-directory: worlds/kirbyam
        run: |
          python build.py --skip-patch
          if [ ! -f kirbyam.apworld ]; then
            echo "kirbyam.apworld was not created" >&2
            exit 1
          fi
          ls -lh kirbyam.apworld

      - name: Build kirbyam.apworld for tagged release with refreshed patch
        if: steps.meta.outputs.release_enabled == 'true'
        working-directory: worlds/kirbyam
        run: |
          python build.py --source-type arg --rom "$(pwd)/kirby_ap_payload/kirby.gba"
          if [ ! -f kirbyam.apworld ]; then
            echo "kirbyam.apworld was not created" >&2
            exit 1
          fi
          if [ ! -f data/base_patch.bsdiff4 ]; then
            echo "base_patch.bsdiff4 was not refreshed" >&2
            exit 1
          fi
          ls -lh kirbyam.apworld data/base_patch.bsdiff4 kirby_ap_payload/patch_rom.log
          sha256sum kirbyam.apworld data/base_patch.bsdiff4

      - name: Stage artifact
        id: package
        shell: bash
        run: |
          mkdir -p dist
          cp worlds/kirbyam/kirbyam.apworld "dist/${{ steps.meta.outputs.apworld_name }}"
          ls -lh dist
          echo "apworld_name=${{ steps.meta.outputs.apworld_name }}" >> "${GITHUB_OUTPUT}"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.package.outputs.apworld_name }}
          path: dist/${{ steps.package.outputs.apworld_name }}
          if-no-files-found: error
          retention-days: 14

  release-apworld:
    if: needs.build-apworld.outputs.release_enabled == 'true'
    runs-on: ubuntu-latest
    needs: build-apworld
    permissions:
      contents: write
    steps:
      - name: Download built artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build-apworld.outputs.apworld_name }}
          path: dist

      - name: Ensure tagged release exists
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ needs.build-apworld.outputs.release_tag }}
          RELEASE_NAME: ${{ needs.build-apworld.outputs.release_name }}
        run: |
          if gh release view "${RELEASE_TAG}" --repo "${GITHUB_REPOSITORY}" >/dev/null 2>&1; then
            echo "Release ${RELEASE_TAG} already exists; preserving existing body/state."
          else
            gh api \
              -X POST \
              "repos/${GITHUB_REPOSITORY}/releases/generate-notes" \
              -f tag_name="${RELEASE_TAG}" \
              -f target_commitish="${GITHUB_SHA}" \
              --jq '.body' > release_notes.md

            gh release create "${RELEASE_TAG}" \
              --repo "${GITHUB_REPOSITORY}" \
              --title "${RELEASE_NAME}" \
              --draft \
              --prerelease \
              --notes-file release_notes.md
          fi

      - name: Prune non-APWorld assets from tagged release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ needs.build-apworld.outputs.release_tag }}
          APWORLD_NAME: ${{ needs.build-apworld.outputs.apworld_name }}
        run: |
          gh release view "${RELEASE_TAG}" \
            --repo "${GITHUB_REPOSITORY}" \
            --json assets \
            --jq '.assets[].name' | while IFS= read -r asset_name; do
              if [ -n "${asset_name}" ] && [ "${asset_name}" != "${APWORLD_NAME}" ]; then
                echo "Deleting prior release asset: ${asset_name}"
                gh release delete-asset "${RELEASE_TAG}" "${asset_name}" --repo "${GITHUB_REPOSITORY}" --yes
              fi
            done

      - name: Upload APWorld to tagged release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ needs.build-apworld.outputs.release_tag }}
          APWORLD_NAME: ${{ needs.build-apworld.outputs.apworld_name }}
        run: |
          gh release upload "${RELEASE_TAG}" "dist/${APWORLD_NAME}" \
            --repo "${GITHUB_REPOSITORY}" \
            --clobber

      - name: Verify release asset publication
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ needs.build-apworld.outputs.release_tag }}
          APWORLD_NAME: ${{ needs.build-apworld.outputs.apworld_name }}
        run: |
          gh release view "${RELEASE_TAG}" \
            --repo "${GITHUB_REPOSITORY}" \
            --json assets \
            --jq '.assets[].name' | grep -Fx "${APWORLD_NAME}"